---
description: 加入互動 JS 元件到頁面
---

# /js

從元件庫中選擇並插入互動 JS 元件。

## 1. 讀取元件庫
// turbo
| 8 | Worldwide Scroll | `s_custom_worldwide` | 上圖下文水平捲軸 | - |
| 9 | Worldwide RWD Collapse | `s_custom_worldwideRWDcollapse` | 桌機捲軸/RWD 收合 | - |

## 3. 輸出

產出三個區塊：
1. **XML** — 元件的 HTML 結構
2. **JavaScript** — 包裝在 `s_embed_code` + `s_custom_noRemove` 中
3. **SCSS** — 元件對應的樣式
