# dynamic

快速加入動態產品或部落格/新聞區塊。

## Steps

1. 讀 `resources/dynamic_rules.md`
2. 以 `templates/base-dynamic-products.xml` / `templates/base-dynamic-news.xml` 為 locked 骨架（不可自行改內部結構）
3. 需要樣式變化時，優先套 `docs/user_custom_rules.scss` 已存在的 `s_custom_*`（查 `resources/scss_reference.md`）
