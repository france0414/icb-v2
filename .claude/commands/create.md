# create (創作模式)

創作全新頁面：先輸出文字骨架（Phase A），使用者確認後才生成 XML + SCSS。

## Steps

1. 讀 ` .agent/skills/icb_page_generator/SKILL.md`
2. 先產出文字骨架：區塊順序、每段目的、內容要點（不輸出 XML/SCSS）
3. 使用者確認後才生成 XML + SCSS
4. 需要 SCSS 工具類時查 `resources/scss_reference.md`
